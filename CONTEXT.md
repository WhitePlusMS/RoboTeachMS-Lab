# ABB 教学编程仿真

本上下文描述浏览器内 ABB 教学程序从运动指令到机器人运动结果的统一语言，用于区分程序语义、运动规划与运动执行。

## Language

**运动执行**：
一条已经通过规划与校验的机器人运动正在按计划推进的过程。同一时刻最多存在一个活动的运动执行。
_Avoid_: 动画任务、动作队列

**完成**：
活动运动到达其计划终点后的正常终止结果，程序可以据此推进到下一条指令。
_Avoid_: 动画结束、成功状态

**停止**：
活动运动在到达计划终点前被用户或上层运行流程终止的结果，程序不得自动推进。
_Avoid_: 失败、暂停、取消

**暂停**：
活动运动暂时冻结但仍然存在的状态；继续后属于同一次运动执行，不产生终止结果。
_Avoid_: 停止、结束

**等待下一步提示**：
单步完成一整条 RAPID 指令后，由“程序已停止且下一执行位置仍在程序内”派生的教学界面提示；它不是独立的 ABB 控制器执行状态。
_Avoid_: 暂停状态、独立执行状态、运动冻结

**程序指针（PP）**：
ABB 程序中下一条将执行指令的位置；本项目不把它与正在运动的指令混为同一指针。
_Avoid_: 当前运动指针、源码光标

**运动指针（MP）**：
ABB 程序中机器人当前正在执行的运动指令位置；它可以与程序指针不同。
_Avoid_: 下一条指令、程序指针

**PP to Main**：
把程序指针移到 main 入口的程序操作；它不让机器人回零，也不清空 Three.js 轨迹。
_Avoid_: 机器人回零、程序暂停、清空轨迹

**偏离路径（off path）**：
程序停止后机器人被手动 Jog，当前姿态不再位于原运动路径上的状态；MVP 再启动时从当前位置规划到下一目标。
_Avoid_: 程序错误、目标不可达、PP to Main

**运动状态**：
MotionRunner 当前是否空闲、正在推进或暂停，只包含 `idle`、`running`、`paused`；它不是 RAPID 程序状态。
_Avoid_: 程序状态、执行结果、状态快照

**运动结果**：
一次运动执行终止时供上层等待的结果，只包含到达终点的 `completed` 和提前终止的 `stopped`。
_Avoid_: paused、replaced、IK failed

**RAPID 文本执行闭环**：
用户输入的真实 RAPID 子集经过解析、诊断和符号解析，转换为结构化运动指令后交给现有程序执行器驱动机器人；它是下一阶段产品里程碑，不等同于完整 RAPID 编译器。
_Avoid_: RAPID-like 脚本、内置程序演示、完整控制器仿真

**RAPID 文本解析模块**：
完整检查 RAPID 源程序并把已支持的名称和值转换成结构化运动指令的模块；它在运动开始前返回可执行程序或诊断，不负责运动规划与运动执行。
_Avoid_: RAPID 编译器、关键词搜索器、机器人运行时

**RAPID 源程序**：
编辑区中作为程序和程序数据唯一事实源的一段 RAPID 文本；解析结果、可执行程序和 Program Data 视图都由它派生，确认后的示教操作以可见的源码编辑更新它。
_Avoid_: 结构化演示数据、隐藏点位状态、双向同步点位表

**程序数据**：
RAPID 模块中具名的数据声明；当前教学范围关注模块级 `CONST robtarget`，它可以先于运动指令独立创建并由 MoveJ/MoveL 按名称引用。界面中的 Program Data 列表只是 RAPID 源程序的派生视图。
_Avoid_: 系统参数、外部点位表、Three.js 目标对象

**点位示教**：
把当前活动工具的 TCP、机器人构型和外轴信息记录为一个 RAPID 运动目标的操作；首期目标是模块级命名 `robtarget`。
_Avoid_: 保存机械法兰显示坐标、保存关节动画、场景拾点

**修改位置**：
ABB Modify Position 的教学语义；用当前 Jog 姿态覆盖选中的 RAPID 运动目标，而不是把机器人移动到目标。共享命名目标被修改时，所有引用继续指向更新后的同一数据。
_Avoid_: 笛卡尔移动、复制目标、运行时变量赋值

**受控源码编辑**：
用户确认一次 Program Data 操作后，对 RAPID 源程序实施的一次符号级最小文本修改；只改变目标声明或已经解析到该声明的引用，其余源码逐字保留。存在 error 时不允许执行。
_Avoid_: 全文字符串替换、自动格式化、隐藏状态同步、容错猜测修改

**诊断**：
词法、语法、名称解析或静态数据校验产生的结构化问题，包含稳定代码、严重级别和源码范围；存在 error 时不得开始程序执行。
_Avoid_: 弹窗字符串、控制台异常、运动规划失败

**运动规划错误**：
合法 RAPID 指令在运行时无法生成可执行运动时的程序失败；它终止当前程序，并通过指令保留的源码范围定位到对应 MoveJ/MoveL。
_Avoid_: 语法诊断、MotionRunner 的 stopped 结果、内部异常

**ABB 机器人基座坐标**：
机器人运动学、RAPID 点位和工具/工件变换使用的唯一空间真值；遵循 ABB 机器人基座的右手坐标语义，不随 Three.js 场景坐标约定变化。
_Avoid_: Three.js 世界坐标、显示坐标、FBX 坐标

**机械法兰位姿**：
六轴机器人运动学链末端的法兰位姿；默认 `tool0` 是机械法兰上的单位变换，用户工具的 TCP 由 `tooldata.tframe` 在此基础上组合得到。
_Avoid_: FBX joint7 位姿、视觉工具位姿、场景末端节点

**场景显示适配**：
把 ABB 机器人基座坐标、机械法兰和关节状态映射到 Three.js/FBX 的显示过程；它不定义 FK、IK、RAPID 点位或 `tool0` 的领域语义。
_Avoid_: 运动学真值、机器人核心坐标、RAPID 坐标

**机器人 profile**：
一个具体机器人型号向通用机器人能力提供的稳定运行契约，聚合型号身份、运动学模型、关节范围和回零关节状态；厂家 DH 参数、三维资产和场景节点不属于该契约。
_Avoid_: 场景配置、FBX 清单、厂家指令集、机器人注册中心

**厂家 adapter**：
把某一厂家具体型号的运动学、关节限制和坐标约定接入通用机器人能力的实现；ABB 与 KUKA 可以分别提供 adapter，但当前产品里程碑只推进 ABB 行为。
_Avoid_: 通用机器人核心、多厂家运行时、场景 adapter
